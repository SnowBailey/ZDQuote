# ZDQuote 运行 + 打包依赖（Windows 10 用）
# 注意：pip 包名 msoffcrypto-tool，import 时名为 msoffcrypto
customtkinter>=5.2
openpyxl>=3.1
msoffcrypto-tool>=5.3
cryptography>=42.0
olefile>=0.47
pyinstaller>=6.0
pillow>=10.0
